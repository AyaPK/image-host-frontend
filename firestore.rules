rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are permissive for LOCAL DEVELOPMENT with the Emulator Suite only.
    // Do NOT deploy these to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
